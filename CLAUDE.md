# CLAUDE.md

You're a Claude agent (Claude Code CLI or Cowork task session) that just spawned in this repo. **The canonical agent contract is `AGENTS.md`.** Read it first.

`AGENTS.md` §0 has the bootstrap reading order — what to read every session, plus per-task reading sequences for slice work vs architectural review.

This file exists to satisfy Claude's auto-bootstrap convention without duplicating content. AGENTS.md is the single source of truth for both Claude (via this redirect) and Codex (which auto-reads AGENTS.md directly).

---

*If you've read AGENTS.md, you've read everything you need from here.*
