# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.vim/

# Node (if your project adopts a Node stack)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# Python (if your project adopts a Python stack)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
ENV/
env/

# Test / coverage
.coverage
.coverage.*
.cache
.nyc_output
coverage/
htmlcov/
*.cover

# Env files (never commit secrets)
.env
.env.local
.env.*.local
.env.production
.env.staging

# Logs
*.log
logs/

# OS / runtime artifacts
*.pid
*.seed
*.pid.lock

# Worktree-owner canary (per worktree, not for the repo)
.wb-owner

# Memory steward heartbeat archive (committed deliberately when archived; not raw mid-flight state)
# (Adjust per your archival policy.)

# Project-specific build / dist outputs
# Add your stack-specific entries here as the project takes shape.
